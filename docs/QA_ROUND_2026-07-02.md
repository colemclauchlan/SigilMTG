# QA Round — 2026-07-02 (Cole's full list, grouped by file cluster)

> Source: Cole's message 2026-07-02. Execute in order within each group. Keep the
> Arcane Foil / Midnight Azure theme + mana-seal branding. Vanilla JS only (no build).
> Log every shipped item in the new What's New changelog (v0.1, layman wording — G6.45).

## G1 — Bottom-left life cluster (play-life.js / play-life.css; small MTGTable API adds)

1. REMOVE the top-right life/vitals panel entirely — the live Stack feature takes that space (see G2.17b).
2. Rename the "Counters" button → **"Player Counters"**.
3. Death button → **"Scoop/Concede"**. Pressing it opens a CENTERED confirm modal: text "Are you sure? This action is irreversible." with a **red Confirm** and **green Cancel** (colored text, highlight on hover).
4. Add **Library / Graveyard / Exile counts** to the bottom-left life area (live, from MTGTable state).
5. Life delta readout: show ONLY "+4" / "-3" style pending integers — no running math equation.
6. Health total **flashes red on loss, green on gain**.
7. Counters UI: add **monarch**. Give EVERY health-relevant counter an icon: energy=lightning, rad=trefoil, experience=star (pick tasteful icons; poison already has one).
8. **Commander damage rework**: kill the Lifelink button, the Cmdr button, and the normal/poison toggle. New UI shows ALL players + their commanders' CARD ART, like the paper tracker's commander-damage UI. (+/- per commander, atomic batch dispatch stays.)
9. Life can NEVER go negative. At 0: the health bar fills red with white "YOU ARE DEAD" text.
10. Holding + accelerates: +1 → +5 → +10 the longer it's held; flash green per tick. Same for − with red flash.
11. Add a **Draw** button (draws 1 from my library).
12. Add a **Roll Dice** button that opens the existing dice-roll UI (the top-bar dice button is being removed — G4.28).
13. Move the Trackers button from the top bar into the bottom-left hub, renamed **"Counters"**, stacked VERTICALLY above the Commander-damage button.

## G2 — Stack + action log + turn flash (table.js / table.css)

14. Stack panel title → **"Stack (Last In First Out)"**.
15. Stack items are **click-drag reorderable** (replaces/augments ▲▼).
16. Clicking the magnifier icon OR the card thumbnail explodes (inspects) the card.
17. Empty stack **fades out** and the action log **animates up** to take its place; when the first card enters the stack it **animates in** and pushes the log down.
17b. The stack lives TOP-RIGHT (space freed by removing the old top-right panel, G1.1).
18. **Pass turn**: screen flashes WHITE + "Turn Passed!" popup that fades. **Receiving turn**: screen flashes YELLOW + "Your turn!" popup that fades.

## G3 — Library / hand / board cards / command zone / graveyard / targeting (table.js)

19. Right-clicking the library must NOT draw a card (menu only). Add **"Mill X…"** (prompt count). Rename "Scry N…" → **"Scry X…"**.
20. Hand overflow: **scroll wheel pans** the hand when it overflows; while hovering a hand card, scrolling moves the hover left/right (cards behave as hovered).
21. Right-click on a HAND card gets its own menu: **Discard, Discard at random, Discard X at random, Move to battlefield, Move to battlefield tapped, Exile**.
22. Board cards: bottom-left **total-power badge** — base P/T + all counters/modifiers = effective P/T, styled like the token count icon (top-right +1/+1 pill is the reference).
23. Command zone: ONLY your commander card(s) may enter it (reject other drops). Clicking the command zone opens a modernized single-card UI: big card display + one **"Send to field"** button.
24. Fix the **Target → draw a line** flow. Add **"Declare blocker"** to card right-click: shows a shield icon on the card AND lets the user draw a BLUE arrow line center-of-card → center-of-target-card.
25. BUG: targeting stays sticky — after one target is drawn it must EXIT targeting mode.
26. Graveyard viewer: modernize the UI/buttons (token-based .svc/.pv styling, icon buttons + tooltips).

## G4 — Top bar / HUD / settings (play-hud.js, table.js toolbar)

27. Redo the icons for legibility; **Engine button = "E"**.
28. REMOVE from top bar: Untap-all, Draw, Dice roll, Mulligan (mulligan lives only in the opening-hand UI).
29. Keep every remaining top-bar feature game/engine-related; redo the toggle-log + stack icons.
30. ADD a **text chat icon** and a **voice chat (headset/mic) icon** to the top bar.
31. Settings additions: **Declare winner** dropdown (pick a player → game over flow); **Draw (tie)** button; **End game** button. REMOVE the standalone End-game button from the bar.
32. Turn engine change: **auto-untap + draw fire when YOUR turn STARTS**, not when you pass the turn.

## G5 — Pregame lobby + mode select (play-shell.js / play-shell.css)

33. FULL lobby overhaul. Remove the Solo/Online toggle. Group the invite-code text input + Join button WITH the invite-friends UI. Remove the redundant Reset-table UI.
34. Top half = **League-draft-style deck select**: your saved decks as square cards (commander art, deck/commander name, bracket icon). Lock In → your player card in the lobby shows deck + bracket + commander for ALL users (each user picks from their own account decks).
35. **"Select a precon"** button → scrollable UI of EVERY WotC precon (names + commander pictures). Reuse the MTGJSON catalog in precons.js (window.MTGPreconsUI / mtg-precons-catalog-v1 cache).
36. Host toggle: **"Precons only"** — disables custom decks for the game.
37. Moxfield import collapses behind an **"Import deck from link"** button (dropdown), not always open.
38. Deck-link import accepts **Moxfield, Archidekt, AND ManaBox** links.
39. Mode-select screen: **Sigil logo top-center**, cosmic/space background with the sigil as a CONSTELLATION. Remove the name input — show the account name as a badge (guests = "Guest" + random integer). Add an **online-players readout**, a **"Spectate"** button (live games), and a **"View all lobbies"** button — both buttons BELOW the gamemodes.

## G6 — Site chrome (index.html / app.js / styles.css / whatsnew.js)

40. Nav tab "Life Counter" → **"Paper Tracker"**; SWAP the positions of Play and Paper Tracker.
41. Remove the dark/light-mode button (keep the default theme).
42. Remove the "local only" element.
43. Color the Sign-in button BLUE like the bug-report + features buttons.
44. **Changelog discipline**: What's New starts at **v0.1** with a date; from now on every change lands there, summarized for laymen (big changes / new features / major fixes).

## G7 — Platform (after G1–G6)

45. Continue engine-system work (deeper keyword coverage — prior queue item 5).
46. Mobile: site works seamlessly on mobile; optimize WITHOUT changing desktop.
47. Codebase bug scan + fixes.
48. (Prior queue) Sideboard swapping; live 2×2 spectator mirror.

## Ambiguities resolved (flag to Cole at checkpoint)
- "remove the untap all and draw" vs "redo the untap all icon": REMOVED both from the top bar (auto-untap+draw now fire at turn start per G4.32); if Untap-all should stay with a new icon instead, say so.
- Settings "draw" = declaring the GAME a draw/tie (not card draw).
