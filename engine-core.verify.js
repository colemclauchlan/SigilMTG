/* TEMP scratch file — SAFE TO DELETE. It was a throwaway mirror of engine-core.js used once to
 * verify a sandbox edit (the mount serves edited files truncated, so bash couldn't load the real
 * engine-core.js). The real module is engine-core.js; nothing references this file. Not loaded by
 * index.html and not picked up by tests/run-all.cjs (which only runs tests/*.cjs). Delete at will. */
